<UserControl x:Class="DeepClaudeAuto.UI.Views.Steps.Step04_BuildModeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:DeepClaudeAuto.UI.Converters"
             Background="#1E1E2E">

    <UserControl.Resources>
        <conv:EqualityConverter x:Key="EqualityConv"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <TextBlock Text="⚙️  Step 4 — 빌드 방식 선택"
                       FontSize="22" FontWeight="Bold" Foreground="#89B4FA"/>
            <TextBlock Text="DeepClaude 서버를 어떤 방식으로 실행할지 선택하세요."
                       FontSize="13" Foreground="#6C7086" Margin="0,4,0,24"/>

            <!-- 빌드 방식 -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="빌드 방식" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <RadioButton Content="소스 빌드 (Python / Rust)"
                                 IsChecked="{Binding SelectedMode,
                                             Converter={StaticResource EqualityConv},
                                             ConverterParameter=Source}"
                                 Foreground="#CDD6F4" Margin="0,0,0,8"/>
                    <RadioButton Content="Docker 컨테이너"
                                 IsChecked="{Binding SelectedMode,
                                             Converter={StaticResource EqualityConv},
                                             ConverterParameter=Docker}"
                                 Foreground="#CDD6F4"/>
                </StackPanel>
            </Border>

            <!-- 저장소 URL -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="저장소 URL" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding RepoUrl, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </Border>

            <!-- 설치 경로 -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="설치 경로" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 Text="{Binding InstallPath, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,8,0"/>
                        <Button Grid.Column="1"
                                Content="찾아보기"
                                Command="{Binding BrowseInstallPathCommand}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Padding="12,6"/>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
