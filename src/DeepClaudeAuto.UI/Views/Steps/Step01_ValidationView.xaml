<UserControl x:Class="DeepClaudeAuto.UI.Views.Steps.Step01_ValidationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E2E">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 타이틀 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="🔍  Step 1 — 설치 환경 검증"
                       FontSize="22" FontWeight="Bold" Foreground="#89B4FA"/>
            <TextBlock Text="DeepClaude 구축에 필요한 의존성을 검사합니다."
                       FontSize="13" Foreground="#6C7086" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- 결과 목록 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Results}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="36"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="160"/>
                                </Grid.ColumnDefinitions>

                                <!-- 상태 아이콘 -->
                                <TextBlock Grid.Column="0"
                                           Text="{Binding Status, Converter={StaticResource StatusToIcon}}"
                                           FontSize="22" VerticalAlignment="Center"/>

                                <!-- 이름 + 설명 -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                                    <TextBlock Text="{Binding Description}" Foreground="#6C7086" FontSize="12"/>
                                </StackPanel>

                                <!-- 버전 + 메시지 -->
                                <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding Message}"
                                               Foreground="{Binding Status, Converter={StaticResource StatusToBrush}}"
                                               FontSize="12" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- 하단 상태 + 버튼 -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="#6C7086" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="검사 취소"
                        Command="{Binding RunCheckCancelCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Visibility="{Binding IsChecking, Converter={StaticResource BoolToVis}}"
                        Margin="0,0,10,0" Width="100"/>
                <Button Content="🔍  검사 시작"
                        Command="{Binding RunCheckCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        IsEnabled="{Binding IsChecking, Converter={StaticResource BoolToVis}, ConverterParameter=Invert}"
                        Width="120"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
